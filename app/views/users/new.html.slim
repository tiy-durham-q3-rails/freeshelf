.row
  .large-12.columns
    h1 Sign up as a new user
    = simple_form_for @user do |f|
      = f.input :name
      = f.input :admin
      = f.input :email do
        = f.input_field :email, type: :email, class: "mailcheck"
        = f.hint "Must be a valid email", style: "display: none"
      .row
        .medium-6.columns
          = f.input :password
        .medium-6.columns
          = f.input :password_confirmation
      = f.button :submit